var id = new URLSearchParams(location.search).get("id") || "alcazar_arquitection";

fetch("https://Thomasthumbs.github.io/vos/files/" + id + ".html")
  .then(e => e.text())
  .then(e => {
    document.open();
    document.write(e);
    document.close();
  });
